---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-conf
data:
  postgres_uri: {{ .Values.config.postgresUri }}
  report_path: {{ .Values.config.reportPath }}
  log_path: {{ .Values.config.logPath }}
  
